# .dots
Combined dotfiles for void, arch, debian and macos

1. Install `git`, `stow`, `wget` and `unzip`.

2. Clone repo.

3. Run `dots.sh` (and `mac-dots.sh` if on a Mac)

4. Goto scripts folder and run required scripts.

***Voila!***
